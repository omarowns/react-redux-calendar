import moment from 'moment'

function currentEvents(state = [], action) {
  switch (action.type) {
    case 'INCREASE_WEEK':

      return state;
    default:
      return state;
  }
}

export default currentEvents;
